# Phase 0 开发文档与阶段流程基线

## 阶段目标

- 参考 `/data/data/com.termux/files/home/astrbot_plugin_selfie_image/Selfie_Image开发.md` 的组织方式，为 `telegram-web` 建立长期开发文档。
- 明确后续开发采用分阶段多会话方式，避免单会话上下文过大。
- 固化主代理 + 子代理协作、阶段进度文档、会话交接文档和阶段 Git 提交流程。

## 完成内容

- 新增 `Telegram_Web开发.md`。
- 新增本阶段进度记录。
- 新增本阶段到 Phase 1 的交接文档和 `docs/handoff/LATEST.md`。
- 将“优先复用 GitHub/npm/PyPI 成熟方案，不从头实现基础能力”写入固定规则。

## 主代理工作

- 检查当前项目结构、README、`app.py` 路由和 `static/js/app.js` 前端调用。
- 检查参考项目开发文档结构。
- 起草并落地适配 `telegram-web` 的开发方案、阶段模板和启动/收尾流程。

## 子代理协作

- 子代理：Explorer `Fermat`
- 任务：只读检查参考项目和当前项目，提炼开发文档要点。
- 输出：确认应复用参考文档的基线、固定边界、结构、验证命令和收尾规则；提醒 `templates/config.html`、`templates/data.html`、`templates/media_view.html` 暂无明显路由，README 中 StringSession 导入导出需要和实际实现校对。
- 采纳情况：已补充 `PYTHONPYCACHEPREFIX` 语法检查命令、入口/数据路径搜索命令，并把 README/StringSession 校对纳入 Phase 1 风险。

## 改动文件

- `Telegram_Web开发.md`
- `docs/progress/2026-07-04-phase-0-docs.md`
- `docs/handoff/2026-07-04-phase-0-to-phase-1.md`
- `docs/handoff/LATEST.md`

## 开源复用评估

- 候选：本阶段只创建开发文档，不引入运行时依赖。
- 结论：无需新增 GitHub/npm/PyPI 包。
- 原因：需求是流程和文档基线；成熟开源复用策略已写入后续阶段规则。

## 验证结果

- `PYTHONPYCACHEPREFIX="${TMPDIR:-$HOME/.cache}/telegram-web-pycache" python -m py_compile app.py`：通过
- `git diff --check`：通过
- `git status --short`：仅显示本阶段新增文档，提交后应清理为干净工作区

## 风险和遗留

- 当前仓库尚无 `.gitignore`，Phase 1 应优先补充，避免 `data/`、`Download/`、`Pictures/`、session 和缓存误提交。
- 当前仓库尚无自动化测试，Phase 1 应建立最小测试骨架。
- README 提到 StringSession / `.session` 导入导出，但需要 Phase 1 和实际实现逐项校对。
- “自动开启新会话”依赖 Codex CLI 或外部调度器能力；仓库内已通过 `docs/handoff/LATEST.md` 提供可续接上下文。

## Git

- 提交信息：`docs: add telegram web development plan`
