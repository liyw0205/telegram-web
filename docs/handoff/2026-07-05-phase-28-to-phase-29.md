# Phase 28 -> Phase 29 交接

## 当前完成进度

- README 的验证章节已同步当前推荐入口：
  - 运行环境预检：`sh -n scripts/diagnose-runtime.sh` 和 `sh scripts/diagnose-runtime.sh`。
  - 自动化回归：`py_compile`、后端 `unittest`、前端 `node --check`、纯 Node smoke 和 `git diff --check`。
  - 可选运行中探测：`TELEGRAM_WEB_DIAGNOSTICS_URL=... sh scripts/diagnose-runtime.sh`。
  - 可选真实浏览器 smoke：先运行 `sh scripts/check-browser-smoke-env.sh`，再按 `docs/browser-smoke.md` 手动检查。
- README 已补齐 `tests/frontend_smoke.js` 当前覆盖分组，包含确认弹窗、媒体查看器、聊天消息、会话列表、登录/API、session/任务确认、下载页和诊断页。
- README 已明确诊断脚本边界：
  - 不读取 `data/config.json`。
  - 不打印 Web Token、StringSession、`.session`、代理凭据或 Telegram API 凭据。
  - Web Token 只保存在 `data/config.json` 时，HTTP 探测可能鉴权失败或探测失败。
- `docs/runtime-runbook.md` 已新增“验证入口总览”，集中指向自动化基线、运行预检、运行中诊断接口探测和手动浏览器 smoke。
- `docs/browser-smoke.md` 和 `scripts/diagnose-runtime.sh` 只读复核，未修改。
- 运行逻辑、模板、前端 JS、后端 API、测试代码和脚本行为未改动。

## 本阶段提交

- commit：提交信息 `phase28: sync validation documentation`；精确 hash 以 `git log -1 --oneline` 为准。
- 主要文件：
  - `README.md`
  - `docs/runtime-runbook.md`
  - `Telegram_Web开发.md`
  - `docs/progress/2026-07-05-phase-28-validation-docs.md`
  - `docs/handoff/2026-07-05-phase-28-to-phase-29.md`
  - `docs/handoff/LATEST.md`

## 已验证

- `sh -n scripts/diagnose-runtime.sh`：通过
- `sh scripts/diagnose-runtime.sh`：通过，`failures=0 warnings=0`
- `TELEGRAM_WEB_HOST=0.0.0.0 sh scripts/diagnose-runtime.sh`：通过，`failures=0 warnings=1`，warning 为未设置环境变量 Web Token
- `TELEGRAM_WEB_DIAGNOSTICS_URL=http://127.0.0.1:9/api/diagnostics sh scripts/diagnose-runtime.sh`：通过，HTTP 探测失败按 warning 处理
- `sh scripts/check-browser-smoke-env.sh`：通过；输出自动化浏览器 smoke 当前不可用，使用手动清单
- `PYTHONPYCACHEPREFIX="${TMPDIR:-$HOME/.cache}/telegram-web-pycache" python -m py_compile app.py`：通过
- `PYTHONPYCACHEPREFIX="${TMPDIR:-$HOME/.cache}/telegram-web-pycache" python -Wd -m unittest discover -v`：通过，56 个测试
- `node --check static/js/app.js`：通过
- `node --check tests/frontend_smoke.js`：通过
- `node tests/frontend_smoke.js`：通过，输出 `frontend smoke passed`
- `git diff --check`：通过
- `git diff --cached --check`：通过

## 未完成/风险

- 未执行真实浏览器或屏幕阅读器人工 smoke。
- 自动浏览器 smoke 仍未引入；如后续引入，需要单独新增 npm 元数据、锁文件、浏览器安装/缓存说明和可选验证边界。
- 诊断脚本仍不会读取 `data/config.json` 中的 Web Token，这是刻意的安全边界。
- 运行数据、Telegram 凭据、session 文件和下载目录仍应保持不提交。

## 下阶段目标

Phase 29 建议主题：**运行文档启动和备份说明一致性复核**。

优先完成：

- 只读复查 `README.md`、`docs/runtime-runbook.md`、`scripts/run-termux.sh` 和登录/session 相关测试，确认启动、对外监听、Web Token、备份和恢复说明一致。
- 如发现 README 与 runbook 对启动命令、Token 优先级、备份对象或恢复步骤描述不一致，优先做文档修正。
- 不改变运行逻辑、API、模板、前端 JS、测试代码或脚本行为，除非发现文档命令明显失效。
- 不引入新依赖，不读取或提交 `Download/`、`Pictures/`、`data/`、`.session` 和任何真实 Token。

## 建议子代理拆分

- Explorer：只读梳理 README、runtime runbook、启动脚本和 session 相关说明是否一致。
- Worker：如确有必要，限定修改文档，不改运行逻辑。
- Verifier：运行诊断脚本、py_compile、后端单测、Node smoke、浏览器环境检查和 `git diff --check`。

## 续会启动步骤

1. 阅读 `Telegram_Web开发.md`。
2. 阅读本文。
3. 运行 `git status --short` 和 `git log --oneline -5`。
4. 确认 Git 身份仍为 `liyw0205 <2650115317@qq.com>`。
5. 按“下阶段目标”启动 Phase 29。
6. 阶段结束前更新新的 `docs/progress/`、`docs/handoff/` 和 `docs/handoff/LATEST.md`，然后提交并推送 Git。
