# Phase 32 -> Phase 33 交接

## 当前完成进度

- 聊天页媒体下载入口文案已与实际后台任务行为对齐：
  - 消息内媒体下载图标可访问名称改为“创建媒体 <id> 下载任务”，并补充 `title="创建下载任务"`。
  - 媒体查看器下载按钮可访问名称改为“创建当前媒体下载任务”。
  - 下载任务创建成功 toast 改为“下载任务已创建，可在下载页查看进度”。
- 媒体预览准备文案已收紧：
  - `/api/media/prepare` 未就绪时显示“媒体正在准备，稍后重试打开”。
  - 单媒体查看器默认标题从英文 `media <id>` 改为中文 `媒体 #<id>`。
- README 已补充聊天页媒体边界：
  - 媒体预览与下载任务为缩略图优先。
  - 聊天页不会自动下载原文件。
  - 预览按需准备缓存；下载按钮创建后台任务并在下载页查看进度。
- `docs/browser-smoke.md` 已补充聊天页缩略图/文件占位、按需准备缓存、未就绪 toast、下载任务创建 toast 和查看器下载按钮边界。
- `docs/runtime-runbook.md` 已新增“聊天页发送和媒体边界”章节，记录文字发送、文件发送、安全 Markdown、加载更早消息、缩略图、媒体准备、查看器和下载任务行为。
- `tests/test_core.py` 和 `tests/frontend_smoke.js` 已同步聊天/媒体文案断言，并新增纯 Node smoke 覆盖媒体准备未就绪、查看器标题和下载任务请求体。
- Telegram 发送流程、媒体缓存/下载流程、图库交互逻辑、Range 响应和任务状态机未修改。

## 本阶段提交

- commit：提交信息 `phase32: align chat media copy`；精确 hash 以 `git log -1 --oneline` 为准。
- 主要文件：
  - `templates/chat.html`
  - `static/js/app.js`
  - `tests/test_core.py`
  - `tests/frontend_smoke.js`
  - `README.md`
  - `docs/browser-smoke.md`
  - `docs/runtime-runbook.md`
  - `Telegram_Web开发.md`
  - `docs/progress/2026-07-05-phase-32-chat-media-copy.md`
  - `docs/handoff/2026-07-05-phase-32-to-phase-33.md`
  - `docs/handoff/LATEST.md`

## 已验证

- `sh -n scripts/diagnose-runtime.sh`：通过
- `sh scripts/diagnose-runtime.sh`：通过，`failures=0 warnings=0`
- `TELEGRAM_WEB_HOST=0.0.0.0 sh scripts/diagnose-runtime.sh`：通过，`failures=0 warnings=1`，warning 为未设置环境变量 Web Token
- `TELEGRAM_WEB_DIAGNOSTICS_URL=http://127.0.0.1:9/api/diagnostics sh scripts/diagnose-runtime.sh`：通过，HTTP 探测失败按 warning 处理
- `sh scripts/check-browser-smoke-env.sh`：通过；输出自动化浏览器 smoke 当前不可用，使用手动清单
- `PYTHONPYCACHEPREFIX="${TMPDIR:-$HOME/.cache}/telegram-web-pycache" python -m py_compile app.py`：通过
- `PYTHONPYCACHEPREFIX="${TMPDIR:-$HOME/.cache}/telegram-web-pycache" python -Wd -m unittest discover -v`：通过，56 个测试
- `node --check static/js/app.js`：通过
- `node --check tests/frontend_smoke.js`：通过
- `node tests/frontend_smoke.js`：通过，输出 `frontend smoke passed`
- `git diff --check`：通过
- `git diff --cached --check`：通过

## 未完成/风险

- 未执行真实浏览器或屏幕阅读器人工 smoke。
- 自动浏览器 smoke 仍未引入；如后续引入，需要单独新增 npm 元数据、锁文件、浏览器安装/缓存说明和可选验证边界。
- 本阶段只改聊天/媒体文案、文档和断言，未改变发送、媒体准备或下载任务行为。
- 媒体准备和下载仍依赖 Telegram 连接状态和后台任务检查点；未就绪时需要稍后重试打开或到下载页查看任务。
- 运行数据、Telegram 凭据、session 文件和下载目录仍应保持不提交。

## 下阶段目标

Phase 33 建议主题：**会话列表和诊断页文案边界复核**。

优先完成：

- 只读复查 `templates/chats.html`、`templates/diagnostics.html`、`static/js/app.js` 中会话列表、搜索、刷新、空状态、诊断项、脱敏字段、复制错误 ID 和 API 错误提示相关文案，及 `README.md`、`docs/browser-smoke.md`、`docs/runtime-runbook.md` 和相关测试。
- 确认会话搜索、刷新状态、未读徽标、空/错误状态、诊断页路径/配置/权限/运行状态、敏感字段脱敏和错误 ID 提示的页面文案、文档和测试描述一致。
- 如发现页面提示、按钮标签、README、runbook 或手动 smoke 清单与实际行为不一致，优先做小范围文案/文档/断言修正。
- 不改变会话加载、诊断采集、脱敏逻辑、鉴权、Socket.IO 或 API 行为，除非发现现有文案对应的命令/行为明显失效。
- 不引入新依赖，不读取或提交 `Download/`、`Pictures/`、`data/`、`.session` 和任何真实 Token。

## 建议子代理拆分

- Explorer：只读梳理会话列表页、诊断页、前端文案、README、runbook、browser smoke 和相关测试之间的一致性。
- Worker：如确有必要，限定修改文案和文档，不改会话加载或诊断行为逻辑。
- Verifier：运行诊断脚本、py_compile、后端单测、Node smoke、浏览器环境检查和 `git diff --check`。

## 续会启动步骤

1. 阅读 `Telegram_Web开发.md`。
2. 阅读本文。
3. 运行 `git status --short` 和 `git log --oneline -5`。
4. 确认 Git 身份仍为 `liyw0205 <2650115317@qq.com>`。
5. 按“下阶段目标”启动 Phase 33。
6. 阶段结束前更新新的 `docs/progress/`、`docs/handoff/` 和 `docs/handoff/LATEST.md`，然后提交并推送 Git。
