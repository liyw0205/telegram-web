# Phase 34 -> Phase 35 交接

## 当前完成进度

- 顶部状态和刷新按钮文案已统一：
  - 初始状态为“Telegram 状态检查中...”。
  - 刷新按钮可访问名称为“刷新 Telegram 连接状态”，并补充 `type="button"`。
  - 状态刷新成功时显示“Telegram 已登录：...”、“Telegram 已连接，未授权”或“Telegram 未连接”。
  - 状态接口错误时展示可操作错误文案。
- 底部导航语义已补齐：
  - 底部导航标记为“主导航”。
  - 会话、下载、诊断、登录入口都有明确可访问名称。
  - 当前页面入口使用 `aria-current="page"`。
- Web Token 验证页语义已补齐：
  - `/auth` 的“访问验证”和“Web Token”区域补充标题关联。
  - Token 输入框有 label 绑定。
  - 错误 Token 提示使用 `role="alert"`。
- Web Token 401 文案已统一为“需要 Web Token，请先验证”：
  - 前端 `api()` 仍跳转 `/auth?next=...`。
  - 后端 API 401 返回同一文案。
- 错误 ID 提示已补清复制边界：
  - 包含 `error_id` 的 API 错误仍保留 `err.errorId`。
  - 支持剪贴板时提示中追加“已尝试复制”。
  - 复制失败不阻断页面操作。
- README、browser smoke、runtime runbook、`Telegram_Web开发.md`、后端静态测试和前端 smoke 已同步。
- 鉴权判断、Socket.IO 鉴权、API 错误包装结构、剪贴板权限处理和导航目标未修改。

## 本阶段提交

- commit：提交信息 `phase34: align global feedback copy`；精确 hash 以 `git log -1 --oneline` 为准。
- 主要文件：
  - `app.py`
  - `templates/base.html`
  - `templates/auth.html`
  - `static/js/app.js`
  - `tests/test_core.py`
  - `tests/frontend_smoke.js`
  - `README.md`
  - `docs/browser-smoke.md`
  - `docs/runtime-runbook.md`
  - `Telegram_Web开发.md`
  - `docs/progress/2026-07-05-phase-34-global-feedback-copy.md`
  - `docs/handoff/2026-07-05-phase-34-to-phase-35.md`
  - `docs/handoff/LATEST.md`

## 已验证

- `sh -n scripts/diagnose-runtime.sh`：通过
- `sh scripts/diagnose-runtime.sh`：通过，`failures=0 warnings=0`
- `TELEGRAM_WEB_HOST=0.0.0.0 sh scripts/diagnose-runtime.sh`：通过，`failures=0 warnings=1`，warning 为未设置环境变量 Web Token
- `TELEGRAM_WEB_DIAGNOSTICS_URL=http://127.0.0.1:9/api/diagnostics sh scripts/diagnose-runtime.sh`：通过，HTTP 探测失败按 warning 处理
- `sh scripts/check-browser-smoke-env.sh`：通过；输出自动化浏览器 smoke 当前不可用，使用手动清单
- `PYTHONPYCACHEPREFIX="${TMPDIR:-$HOME/.cache}/telegram-web-pycache" python -m py_compile app.py`：通过
- `PYTHONPYCACHEPREFIX="${TMPDIR:-$HOME/.cache}/telegram-web-pycache" python -Wd -m unittest discover -v`：通过，57 个测试
- `node --check static/js/app.js`：通过
- `node --check tests/frontend_smoke.js`：通过
- `node tests/frontend_smoke.js`：通过，输出 `frontend smoke passed`
- `git diff --check`：通过
- `git diff --cached --check`：通过

## 未完成/风险

- 未执行真实浏览器或屏幕阅读器人工 smoke。
- 自动浏览器 smoke 仍未引入；如后续引入，需要单独新增 npm 元数据、锁文件、浏览器安装/缓存说明和可选验证边界。
- 错误 ID 复制仍取决于浏览器剪贴板权限；复制失败不会阻断页面操作。
- 顶部状态仍只展示摘要，不替代日志、诊断页或 Telegram 登录回归。
- 运行数据、Telegram 凭据、session 文件和下载目录仍应保持不提交。

## 下阶段目标

Phase 35 建议主题：**配置校验和 API 参数错误文案边界复核**。

优先完成：

- 只读复查 `app.py` 中配置保存、分页参数、Range 请求、session 文件名、代理解析、下载线程/缓存上限和 JSON 请求错误相关文案，及 `README.md`、`docs/runtime-runbook.md`、`docs/browser-smoke.md` 和相关测试。
- 确认 `api_id`、`api_hash`、手机号、代理、Session 文件名、Web Token、下载线程、缓存上限、分页参数、Range 请求和 JSON 请求错误的后端文案、前端提示、文档和测试描述一致。
- 如发现页面提示、API 错误、README、runbook 或测试断言与实际行为不一致，优先做小范围文案/文档/断言修正。
- 不改变配置校验规则、文件路径约束、Range 解析、分页逻辑、鉴权逻辑或 Telethon 行为，除非发现现有文案对应的命令/行为明显失效。
- 不引入新依赖，不读取或提交 `Download/`、`Pictures/`、`data/`、`.session` 和任何真实 Token。

## 建议子代理拆分

- Explorer：只读梳理配置/参数校验入口、错误文案、README、runbook、browser smoke 和相关测试之间的一致性。
- Worker：如确有必要，限定修改文案和文档，不改校验规则、路径约束、Range 或分页行为逻辑。
- Verifier：运行诊断脚本、py_compile、后端单测、Node smoke、浏览器环境检查和 `git diff --check`。

## 续会启动步骤

1. 阅读 `Telegram_Web开发.md`。
2. 阅读本文。
3. 运行 `git status --short` 和 `git log --oneline -5`。
4. 确认 Git 身份仍为 `liyw0205 <2650115317@qq.com>`。
5. 按“下阶段目标”启动 Phase 35。
6. 阶段结束前更新新的 `docs/progress/`、`docs/handoff/` 和 `docs/handoff/LATEST.md`，然后提交并推送 Git。
