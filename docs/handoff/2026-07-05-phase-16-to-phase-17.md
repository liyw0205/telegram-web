# Phase 16 -> Phase 17 交接

## 当前完成进度

- 已评估当前 shell 的真实浏览器自动化条件。
- `node`、`npm`、`npx` 可用，但缺少：
  - `playwright` 命令
  - Node 侧 `playwright` 模块
  - `chromium`
  - `chromium-browser`
  - `google-chrome`
  - `google-chrome-stable`
  - `firefox`
- 仓库没有 `package.json`、锁文件、Playwright 配置或浏览器 smoke 脚本。
- 新增 `scripts/check-browser-smoke-env.sh`，用于重复检查本机浏览器 smoke 条件。
- 新增 `docs/browser-smoke.md`，记录必跑自动化基线、手动浏览器 smoke 清单和后续 Playwright 引入边界。
- README 和 `Telegram_Web开发.md` 已同步说明真实浏览器 smoke 当前是可选手动验证。
- 本阶段不引入新 npm 依赖，不修改业务行为。

## 本阶段提交

- commit：提交信息 `phase16: document browser smoke boundary`；精确 hash 以 `git log -1 --oneline` 为准。
- 主要文件：
  - `scripts/check-browser-smoke-env.sh`
  - `docs/browser-smoke.md`
  - `README.md`
  - `Telegram_Web开发.md`
  - `docs/progress/2026-07-05-phase-16-browser-smoke-boundary.md`
  - `docs/handoff/2026-07-05-phase-16-to-phase-17.md`
  - `docs/handoff/LATEST.md`

## 已验证

- `sh scripts/check-browser-smoke-env.sh`：通过，输出当前 shell 不具备自动浏览器 smoke 条件
- `PYTHONPYCACHEPREFIX="${TMPDIR:-$HOME/.cache}/telegram-web-pycache" python -m py_compile app.py`：通过
- `PYTHONPYCACHEPREFIX="${TMPDIR:-$HOME/.cache}/telegram-web-pycache" python -Wd -m unittest discover -v`：通过，48 个测试
- `node --check static/js/app.js`：通过
- `node --check tests/frontend_smoke.js`：通过
- `node tests/frontend_smoke.js`：通过，输出 `frontend smoke passed`
- `git diff --check`：通过

## 未完成/风险

- 未执行自动真实浏览器回归；当前 shell 缺少浏览器和 Playwright 条件。
- 手动 smoke 清单需要人工执行，无法捕获所有布局或触摸回归。
- 无 systemd、Termux service、容器化或日志轮转脚本。
- 运行数据、Telegram 凭据、session 文件和下载目录仍应保持不提交。

## 下阶段目标

Phase 17 建议主题：**运行诊断和服务化文档收口**。

优先完成：

- 梳理当前启动、访问、Web Token、日志观察和常见故障排查路径。
- 补充最小运行诊断文档或脚本，优先面向 Termux/本机单进程部署。
- 不新增后台守护进程依赖；如果评估 systemd/Termux service，只记录边界和手动配置建议。
- 不触碰真实 `data/`、`Download/`、`Pictures/`、session、Token 或 Telegram 凭据。
- 保持 py_compile、后端单测、Node smoke 和 `git diff --check` 必跑。

## 建议子代理拆分

- Explorer：只读梳理 README、脚本、配置项、启动环境变量和现有错误日志说明。
- Worker：补充运行诊断文档或轻量脚本，限定修改 `README.md`、`docs/`、`scripts/` 和必要开发方案。
- Verifier：运行 py_compile、单元测试、Node smoke、`git diff --check`，确认没有运行数据进入提交。

## 续会启动步骤

1. 阅读 `Telegram_Web开发.md`。
2. 阅读本文。
3. 运行 `git status --short` 和 `git log --oneline -5`。
4. 确认 Git 身份仍为 `liyw0205 <2650115317@qq.com>`。
5. 按“下阶段目标”启动 Phase 17。
6. 阶段结束前更新新的 `docs/progress/`、`docs/handoff/` 和 `docs/handoff/LATEST.md`，然后提交 Git。
