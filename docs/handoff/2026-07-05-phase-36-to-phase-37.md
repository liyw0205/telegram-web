# Phase 36 -> Phase 37 交接

## 当前完成进度

- `Telegram_Web开发.md` 已重生成：
  - 当前基线更新为 Phase 36。
  - 结构从旧的累积式开发方案调整为工程手册式开发文档。
  - 集中说明项目定位、固定边界、工作区结构、运行数据、配置校验、API/页面入口、核心工作流、前端交互基线、测试验证、开源复用、阶段制度、文档模板、检索命令、阶段摘要和后续建议。
  - 保留 Phase 0-36 阶段摘要，但详细历史仍以 `docs/progress/` 和 `docs/handoff/` 为准。
- 未改应用代码、测试代码、前端行为、配置规则、鉴权逻辑、Telethon 行为或依赖。
- `docs/progress/2026-07-05-phase-36-regenerate-dev-doc.md` 已新增。
- `docs/handoff/LATEST.md` 已更新为本文内容。

## 本阶段提交

- commit：提交信息 `phase36: regenerate development docs`；精确 hash 以 `git log -1 --oneline` 为准。
- 主要文件：
  - `Telegram_Web开发.md`
  - `docs/progress/2026-07-05-phase-36-regenerate-dev-doc.md`
  - `docs/handoff/2026-07-05-phase-36-to-phase-37.md`
  - `docs/handoff/LATEST.md`

## 已验证

- `sh -n scripts/diagnose-runtime.sh`：通过
- `sh scripts/diagnose-runtime.sh`：通过，`failures=0 warnings=0`
- `TELEGRAM_WEB_HOST=0.0.0.0 sh scripts/diagnose-runtime.sh`：通过，`failures=0 warnings=1`，warning 为未设置环境变量 Web Token
- `TELEGRAM_WEB_DIAGNOSTICS_URL=http://127.0.0.1:9/api/diagnostics sh scripts/diagnose-runtime.sh`：通过，HTTP 探测失败按 warning 处理
- `sh scripts/check-browser-smoke-env.sh`：通过；输出自动化浏览器 smoke 当前不可用，使用手动清单
- `PYTHONPYCACHEPREFIX="${TMPDIR:-$HOME/.cache}/telegram-web-pycache" python -m py_compile app.py`：通过
- `PYTHONPYCACHEPREFIX="${TMPDIR:-$HOME/.cache}/telegram-web-pycache" python -Wd -m unittest discover -v`：通过，57 个测试
- `node --check static/js/app.js`：通过
- `node --check tests/frontend_smoke.js`：通过
- `node tests/frontend_smoke.js`：通过，输出 `frontend smoke passed`
- `git diff --check`：通过
- `git diff --cached --check`：通过

## 未完成/风险

- 未执行真实浏览器或屏幕阅读器人工 smoke。
- 主开发文档已压缩历史阶段详情；需要查具体阶段实现细节时继续查看 `docs/progress/` 和 `docs/handoff/`。
- 自动浏览器 smoke 仍未引入；当前环境检查仍显示缺少 Playwright 模块和常见浏览器命令。
- 运行数据、Telegram 凭据、session 文件和下载目录仍应保持不提交。

## 下阶段目标

Phase 37 建议主题：**登录页配置输入提示和前端错误展示边界复核**。

优先完成：

- 只读复查 `templates/login.html` 和 `static/js/app.js` 中配置输入控件、placeholder、隐藏说明、`loginConfigPayload()`、保存配置/发送验证码错误展示，以及相关 README、runbook、browser smoke 和测试。
- 确认前端输入提示与 Phase 35 后端边界一致：`api_id`、`api_hash`、手机号、SOCKS4/5 代理、Session 文件名、StringSession、下载线程、缓存上限和 Web Token。
- 如发现前端 placeholder、辅助说明、toast 或 smoke 断言与后端实际错误不一致，优先做小范围文案/断言修正。
- 谨慎评估是否需要 HTML 原生 `min/max/pattern/inputmode` 补充；如会改变提交行为，先只记录风险或限制在低风险提示属性。
- 不改变登录流程、配置保存规则、session 导入导出、鉴权逻辑、Telethon 行为或新增依赖。
- 不读取或提交 `Download/`、`Pictures/`、`data/`、`.session` 和任何真实 Token。

## 建议子代理拆分

- Explorer：只读梳理登录页配置控件、前端 payload、后端校验文案和文档/测试之间的一致性。
- Worker：如确有必要，限定修改登录页文案、轻量 HTML 属性、README/runbook/browser smoke 和测试断言，不改登录 API 或配置规则。
- Verifier：运行诊断脚本、py_compile、后端单测、Node smoke、浏览器环境检查和 `git diff --check`。

## 续会启动步骤

1. 阅读 `Telegram_Web开发.md`。
2. 阅读本文。
3. 运行 `git status --short` 和 `git log --oneline -5`。
4. 确认 Git 身份仍为 `liyw0205 <2650115317@qq.com>`。
5. 按“下阶段目标”启动 Phase 37。
6. 阶段结束前更新新的 `docs/progress/`、`docs/handoff/` 和 `docs/handoff/LATEST.md`，然后提交并推送 Git。
