# Phase 17 -> Phase 18 交接

## 当前完成进度

- 新增 `scripts/diagnose-runtime.sh`，用于本机运行预检。
- 诊断脚本覆盖：
  - 仓库关键文件存在性。
  - `python`、`node` 命令。
  - 有效 host、port 和 Web Token 环境变量形状。
  - 非 loopback 监听但无 Token 环境变量时给出 warning。
  - `app.py` 语法编译。
  - Flask、Flask-SocketIO、Telethon、PySocks、python-socketio 依赖导入。
  - `static/js/app.js` 和 `tests/frontend_smoke.js` 语法检查。
- 诊断脚本不读取 `data/config.json`，不打印 Token、StringSession、`.session`、代理凭据或 Telegram API 凭据。
- 新增 `docs/runtime-runbook.md`，覆盖启动、访问、Web Token、日志、错误 ID、常见故障和服务化边界。
- README 和 `Telegram_Web开发.md` 已同步入口。
- 本阶段不新增 systemd、Termux service、容器化或日志轮转依赖，不修改业务行为。

## 本阶段提交

- commit：提交信息 `phase17: add runtime diagnostics runbook`；精确 hash 以 `git log -1 --oneline` 为准。
- 主要文件：
  - `scripts/diagnose-runtime.sh`
  - `docs/runtime-runbook.md`
  - `README.md`
  - `Telegram_Web开发.md`
  - `docs/progress/2026-07-05-phase-17-runtime-diagnostics.md`
  - `docs/handoff/2026-07-05-phase-17-to-phase-18.md`
  - `docs/handoff/LATEST.md`

## 已验证

- `sh -n scripts/diagnose-runtime.sh`：通过
- `sh scripts/diagnose-runtime.sh`：通过，`failures=0 warnings=0`
- `TELEGRAM_WEB_HOST=0.0.0.0 sh scripts/diagnose-runtime.sh`：通过，输出未设置 Token 环境变量的 warning
- `PYTHONPYCACHEPREFIX="${TMPDIR:-$HOME/.cache}/telegram-web-pycache" python -m py_compile app.py`：通过
- `PYTHONPYCACHEPREFIX="${TMPDIR:-$HOME/.cache}/telegram-web-pycache" python -Wd -m unittest discover -v`：通过，48 个测试
- `node --check static/js/app.js`：通过
- `node --check tests/frontend_smoke.js`：通过
- `node tests/frontend_smoke.js`：通过，输出 `frontend smoke passed`
- `git diff --check`：通过

## 未完成/风险

- 诊断脚本不读取本地配置，因此无法确认 `data/config.json` 中是否保存了 Web Token。
- 仍未提供守护进程、容器化或日志轮转模板。
- 真实浏览器和真实 Telegram 登录回归仍依赖手动清单。
- 运行数据、Telegram 凭据、session 文件和下载目录仍应保持不提交。

## 下阶段目标

Phase 18 建议主题：**后端配置和诊断可观测性补强**。

优先完成：

- 评估是否需要新增只返回脱敏状态的本地诊断 API 或 CLI 输出，例如配置是否存在、Token 是否配置、session 类型、下载线程和缓存上限。
- 如果实现，必须复用现有脱敏规则，不返回真实 `api_hash`、StringSession、`.session` 文件内容、Token 或代理凭据。
- 可考虑给 `scripts/diagnose-runtime.sh` 增加可选 HTTP 探测，但必须避免打印真实 Token。
- 保持不触碰真实 `data/`、`Download/`、`Pictures/` 和凭据内容。
- 保持 py_compile、后端单测、Node smoke 和 `git diff --check` 必跑。

## 建议子代理拆分

- Explorer：只读梳理现有 `/api/config`、`/api/status`、脱敏 helper 和测试覆盖。
- Worker：如有必要，新增脱敏诊断接口或脚本可选 HTTP 探测，限定修改 `app.py`、`tests/`、`scripts/` 和文档。
- Verifier：运行 py_compile、单元测试、Node smoke、诊断脚本和 `git diff --check`，确认没有秘密值进入响应或提交。

## 续会启动步骤

1. 阅读 `Telegram_Web开发.md`。
2. 阅读本文。
3. 运行 `git status --short` 和 `git log --oneline -5`。
4. 确认 Git 身份仍为 `liyw0205 <2650115317@qq.com>`。
5. 按“下阶段目标”启动 Phase 18。
6. 阶段结束前更新新的 `docs/progress/`、`docs/handoff/` 和 `docs/handoff/LATEST.md`，然后提交 Git。
