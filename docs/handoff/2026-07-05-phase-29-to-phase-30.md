# Phase 29 -> Phase 30 交接

## 当前完成进度

- README 与运行 runbook 的启动和备份说明已对齐：
  - 对外监听必须已有有效 Web Token。
  - 推荐用 `TELEGRAM_WEB_TOKEN` 环境变量对外访问。
  - Token 来源优先级为 `TELEGRAM_WEB_TOKEN`、`WEB_TELEGRAM_TOKEN`、`data/config.json` 中保存的 `web_token`。
  - 如只设置 `TELEGRAM_WEB_HOST=0.0.0.0` 和端口，需要先在本机 `/login` 保存 Web Token。
- README 的部署和备份说明已补充：
  - `data/config.json` 可能包含 `api_hash`、手机号、代理、StringSession 和 Web Token。
  - 默认文件 session 是 `data/telegram.session`。
  - 自定义或导入文件 session 时，备份对应的 `data/*.session` 文件。
  - 恢复后如果启用 Web Token，需要先通过 `/auth`，再确认 `/api/status`。
- `docs/runtime-runbook.md` 已新增“备份和恢复”章节：
  - 核心备份对象：`data/config.json`、当前 `data/*.session`。
  - 可选备份对象：`data/task-history.json`、`Download/`、`Pictures/`。
  - 恢复流程：安装依赖、默认本机监听、放回配置/session 或页面导入、Web Token 验证、确认授权状态。
- `scripts/run-termux.sh`、`app.py`、`tests/` 只读复核，未修改。
- 运行逻辑、模板、前端 JS、后端 API、测试代码和脚本行为未改动。

## 本阶段提交

- commit：提交信息 `phase29: align startup and backup docs`；精确 hash 以 `git log -1 --oneline` 为准。
- 主要文件：
  - `README.md`
  - `docs/runtime-runbook.md`
  - `Telegram_Web开发.md`
  - `docs/progress/2026-07-05-phase-29-startup-backup-docs.md`
  - `docs/handoff/2026-07-05-phase-29-to-phase-30.md`
  - `docs/handoff/LATEST.md`

## 已验证

- `sh -n scripts/diagnose-runtime.sh`：通过
- `sh scripts/diagnose-runtime.sh`：通过，`failures=0 warnings=0`
- `TELEGRAM_WEB_HOST=0.0.0.0 sh scripts/diagnose-runtime.sh`：通过，`failures=0 warnings=1`，warning 为未设置环境变量 Web Token
- `TELEGRAM_WEB_DIAGNOSTICS_URL=http://127.0.0.1:9/api/diagnostics sh scripts/diagnose-runtime.sh`：通过，HTTP 探测失败按 warning 处理
- `sh scripts/check-browser-smoke-env.sh`：通过；输出自动化浏览器 smoke 当前不可用，使用手动清单
- `PYTHONPYCACHEPREFIX="${TMPDIR:-$HOME/.cache}/telegram-web-pycache" python -m py_compile app.py`：通过
- `PYTHONPYCACHEPREFIX="${TMPDIR:-$HOME/.cache}/telegram-web-pycache" python -Wd -m unittest discover -v`：通过，56 个测试
- `node --check static/js/app.js`：通过
- `node --check tests/frontend_smoke.js`：通过
- `node tests/frontend_smoke.js`：通过，输出 `frontend smoke passed`
- `git diff --check`：通过
- `git diff --cached --check`：通过

## 未完成/风险

- 未执行真实浏览器或屏幕阅读器人工 smoke。
- 未新增备份/恢复自动化；备份仍由用户自行在仓库外完成。
- 自动浏览器 smoke 仍未引入；如后续引入，需要单独新增 npm 元数据、锁文件、浏览器安装/缓存说明和可选验证边界。
- 运行数据、Telegram 凭据、session 文件和下载目录仍应保持不提交。

## 下阶段目标

Phase 30 建议主题：**登录和 session 操作文案一致性复核**。

优先完成：

- 只读复查 `templates/login.html`、`static/js/app.js`、`README.md`、`docs/runtime-runbook.md` 和 session 相关测试，确认登录页上的 StringSession、`.session`、Web Token、导入导出和一次性令牌文案与实际行为一致。
- 如发现页面提示、toast、确认弹窗、README 或 runbook 对导入/导出流程、令牌 TTL、敏感字段留空沿用逻辑描述不一致，优先做小范围文案或文档修正。
- 不改变 Telegram 登录流程、session 存储逻辑、Web Token 鉴权逻辑或测试 harness，除非发现现有文案对应的命令/行为明显失效。
- 不引入新依赖，不读取或提交 `Download/`、`Pictures/`、`data/`、`.session` 和任何真实 Token。

## 建议子代理拆分

- Explorer：只读梳理登录模板、前端文案、README、runbook 和 session 测试之间的一致性。
- Worker：如确有必要，限定修改文案和文档，不改行为逻辑。
- Verifier：运行诊断脚本、py_compile、后端单测、Node smoke、浏览器环境检查和 `git diff --check`。

## 续会启动步骤

1. 阅读 `Telegram_Web开发.md`。
2. 阅读本文。
3. 运行 `git status --short` 和 `git log --oneline -5`。
4. 确认 Git 身份仍为 `liyw0205 <2650115317@qq.com>`。
5. 按“下阶段目标”启动 Phase 30。
6. 阶段结束前更新新的 `docs/progress/`、`docs/handoff/` 和 `docs/handoff/LATEST.md`，然后提交并推送 Git。
