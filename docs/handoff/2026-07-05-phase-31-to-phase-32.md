# Phase 31 -> Phase 32 交接

## 当前完成进度

- 下载页任务操作文案已与实际行为对齐：
  - 暂停/恢复直接执行，不弹确认。
  - 取消排队中、运行中或已暂停任务前弹确认；确认后移除任务记录并向后台任务发送取消信号。
  - 移除已完成、失败或已取消任务记录前弹确认；确认后只移除记录，不删除已下载文件。
- 下载页任务卡片已不再暴露内部枚举：
  - `download_media` 显示为 `下载原文件`。
  - `prepare_media` 显示为 `准备预览`。
  - `queued/running/paused/done/error/canceled` 显示为中文状态。
  - 活跃任务按钮显示为“取消任务”，终态记录按钮显示为“移除记录”。
- 下载页标题说明已补齐“取消任务”和“移除终态记录”。
- README 已补充下载任务管理能力、`.part` 临时文件跳过、分页列出 `Download/` 和 `Pictures/`、取消/移除确认边界。
- `docs/browser-smoke.md` 已修正下载页手动 smoke：暂停/恢复直接执行；取消活跃任务或移除终态记录才弹确认。
- `docs/runtime-runbook.md` 已新增“下载页任务和文件边界”章节，记录任务列表、文件列表、操作边界、终态历史、分页排序、`.part`/符号链接跳过和错误 ID 提示。
- `tests/test_core.py` 和 `tests/frontend_smoke.js` 已同步下载页文案断言。
- 下载任务状态机、文件枚举、分页 API、Range 响应和缓存清理逻辑未修改。

## 本阶段提交

- commit：提交信息 `phase31: align download task copy`；精确 hash 以 `git log -1 --oneline` 为准。
- 主要文件：
  - `templates/downloads.html`
  - `static/js/app.js`
  - `tests/test_core.py`
  - `tests/frontend_smoke.js`
  - `README.md`
  - `docs/browser-smoke.md`
  - `docs/runtime-runbook.md`
  - `Telegram_Web开发.md`
  - `docs/progress/2026-07-05-phase-31-download-copy-boundaries.md`
  - `docs/handoff/2026-07-05-phase-31-to-phase-32.md`
  - `docs/handoff/LATEST.md`

## 已验证

- `sh -n scripts/diagnose-runtime.sh`：通过
- `sh scripts/diagnose-runtime.sh`：通过，`failures=0 warnings=0`
- `TELEGRAM_WEB_HOST=0.0.0.0 sh scripts/diagnose-runtime.sh`：通过，`failures=0 warnings=1`，warning 为未设置环境变量 Web Token
- `TELEGRAM_WEB_DIAGNOSTICS_URL=http://127.0.0.1:9/api/diagnostics sh scripts/diagnose-runtime.sh`：通过，HTTP 探测失败按 warning 处理
- `sh scripts/check-browser-smoke-env.sh`：通过；输出自动化浏览器 smoke 当前不可用，使用手动清单
- `PYTHONPYCACHEPREFIX="${TMPDIR:-$HOME/.cache}/telegram-web-pycache" python -m py_compile app.py`：通过
- `PYTHONPYCACHEPREFIX="${TMPDIR:-$HOME/.cache}/telegram-web-pycache" python -Wd -m unittest discover -v`：通过，56 个测试
- `node --check static/js/app.js`：通过
- `node --check tests/frontend_smoke.js`：通过
- `node tests/frontend_smoke.js`：通过，输出 `frontend smoke passed`
- `git diff --check`：通过
- `git diff --cached --check`：通过

## 未完成/风险

- 未执行真实浏览器或屏幕阅读器人工 smoke。
- 自动浏览器 smoke 仍未引入；如后续引入，需要单独新增 npm 元数据、锁文件、浏览器安装/缓存说明和可选验证边界。
- 取消活跃任务会立即移除前端任务记录并向后台发送取消信号；后台任务实际停止仍取决于下载/预览流程中的取消检查点。
- 运行数据、Telegram 凭据、session 文件和下载目录仍应保持不提交。

## 下阶段目标

Phase 32 建议主题：**聊天页发送和媒体操作文案边界复核**。

优先完成：

- 只读复查 `templates/chat.html`、`static/js/app.js` 中聊天发送、媒体预览、媒体下载、图库查看器相关文案，及 `README.md`、`docs/browser-smoke.md`、`docs/runtime-runbook.md` 和聊天/媒体相关测试。
- 确认文字发送、文件发送、媒体预览、缩略图加载、下载任务创建、图库打开/关闭/下载、加载更早消息和错误 ID 提示的页面文案、文档和测试描述一致。
- 如发现页面提示、按钮标签、确认/状态文案、README、runbook 或手动 smoke 清单与实际行为不一致，优先做小范围文案/文档/断言修正。
- 不改变 Telegram 发送流程、媒体缓存/下载流程、图库交互逻辑、Range 响应或任务状态机，除非发现现有文案对应的命令/行为明显失效。
- 不引入新依赖，不读取或提交 `Download/`、`Pictures/`、`data/`、`.session` 和任何真实 Token。

## 建议子代理拆分

- Explorer：只读梳理聊天模板、前端聊天/媒体文案、README、runbook、browser smoke 和聊天/媒体相关测试之间的一致性。
- Worker：如确有必要，限定修改文案和文档，不改聊天或媒体行为逻辑。
- Verifier：运行诊断脚本、py_compile、后端单测、Node smoke、浏览器环境检查和 `git diff --check`。

## 续会启动步骤

1. 阅读 `Telegram_Web开发.md`。
2. 阅读本文。
3. 运行 `git status --short` 和 `git log --oneline -5`。
4. 确认 Git 身份仍为 `liyw0205 <2650115317@qq.com>`。
5. 按“下阶段目标”启动 Phase 32。
6. 阶段结束前更新新的 `docs/progress/`、`docs/handoff/` 和 `docs/handoff/LATEST.md`，然后提交并推送 Git。
