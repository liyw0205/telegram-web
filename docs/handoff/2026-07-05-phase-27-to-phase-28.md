# Phase 27 -> Phase 28 交接

## 当前完成进度

- 手动浏览器 smoke 清单已收口 Phase 21-26 的可访问性改动：
  - 全局反馈和确认：顶部状态、toast、自定义确认弹窗焦点和键盘行为。
  - 登录页：标题区域、表单标签、敏感字段脱敏和操作分组。
  - 会话列表页：标题、刷新、搜索、会话项、类型和未读徽标。
  - 聊天页：消息列表、操作栏、文字/文件发送区、加载/错误状态和发送后行为。
  - 下载页：刷新/加载按钮、列表状态、分页状态和确认边界。
  - 诊断页：继续保留脱敏诊断和刷新/失败状态检查。
  - 媒体查看器：标题/索引、图标按钮用途、方向键、Esc 和焦点循环。
- `docs/browser-smoke.md` 已记录 Phase 27 仍不引入 Playwright、Puppeteer、Selenium、浏览器安装脚本或真实账号数据。
- `scripts/check-browser-smoke-env.sh` 只读复核，无逻辑改动。
- `Telegram_Web开发.md` 已同步 Phase 27 记录。
- 运行逻辑、模板、前端 JS、后端 API 和测试代码未改动。

## 本阶段提交

- commit：提交信息 `phase27: document accessibility browser smoke`；精确 hash 以 `git log -1 --oneline` 为准。
- 主要文件：
  - `docs/browser-smoke.md`
  - `Telegram_Web开发.md`
  - `docs/progress/2026-07-05-phase-27-accessibility-browser-smoke.md`
  - `docs/handoff/2026-07-05-phase-27-to-phase-28.md`
  - `docs/handoff/LATEST.md`

## 已验证

- `node --check static/js/app.js`：通过
- `node --check tests/frontend_smoke.js`：通过
- `node tests/frontend_smoke.js`：通过，输出 `frontend smoke passed`
- `sh -n scripts/diagnose-runtime.sh`：通过
- `sh scripts/diagnose-runtime.sh`：通过，`failures=0 warnings=0`
- `TELEGRAM_WEB_HOST=0.0.0.0 sh scripts/diagnose-runtime.sh`：通过，`failures=0 warnings=1`，warning 为未设置环境变量 Web Token
- `TELEGRAM_WEB_DIAGNOSTICS_URL=http://127.0.0.1:9/api/diagnostics sh scripts/diagnose-runtime.sh`：通过，HTTP 探测失败按 warning 处理
- `sh scripts/check-browser-smoke-env.sh`：通过；输出自动化浏览器 smoke 当前不可用，使用手动清单
- `PYTHONPYCACHEPREFIX="${TMPDIR:-$HOME/.cache}/telegram-web-pycache" python -m py_compile app.py`：通过
- `PYTHONPYCACHEPREFIX="${TMPDIR:-$HOME/.cache}/telegram-web-pycache" python -Wd -m unittest discover -v`：通过，56 个测试
- `git diff --check`：通过

## 未完成/风险

- 未执行真实浏览器或屏幕阅读器人工 smoke。
- 手动清单不能替代真实浏览器或辅助技术验证。
- 自动浏览器 smoke 仍未引入；如后续引入，需要单独新增 npm 元数据、锁文件和浏览器安装/缓存说明。
- 运行数据、Telegram 凭据、session 文件和下载目录仍应保持不提交。

## 下阶段目标

Phase 28 建议主题：**README 和运行文档验证入口同步**。

优先完成：

- 只读复查 `README.md`、`docs/runtime-runbook.md`、`docs/browser-smoke.md` 和 `scripts/diagnose-runtime.sh` 的验证入口说明。
- 将当前推荐验证命令、手动浏览器 smoke 文档入口、诊断脚本边界和 Web Token 注意事项同步到用户最容易看到的文档位置。
- 不改变运行逻辑、API、模板、前端 JS、测试代码或脚本行为，除非发现文档命令明显失效。
- 不引入新依赖，不读取或提交 `Download/`、`Pictures/`、`data/`、`.session` 和任何真实 Token。

## 建议子代理拆分

- Explorer：只读梳理 README、runtime runbook、browser smoke 文档和诊断脚本说明是否一致。
- Worker：如确有必要，限定修改文档，不改运行逻辑。
- Verifier：运行诊断脚本、py_compile、后端单测、Node smoke、浏览器环境检查和 `git diff --check`。

## 续会启动步骤

1. 阅读 `Telegram_Web开发.md`。
2. 阅读本文。
3. 运行 `git status --short` 和 `git log --oneline -5`。
4. 确认 Git 身份仍为 `liyw0205 <2650115317@qq.com>`。
5. 按“下阶段目标”启动 Phase 28。
6. 阶段结束前更新新的 `docs/progress/`、`docs/handoff/` 和 `docs/handoff/LATEST.md`，然后提交 Git。
