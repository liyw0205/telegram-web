# Phase 33 -> Phase 34 交接

## 当前完成进度

- 会话列表搜索提示已与实际前端过滤字段对齐：
  - 搜索输入占位符和可访问名称补充 `peer`。
  - README、browser smoke 和 runbook 同步说明名称、用户名、peer 和 ID 都可在已加载结果中本地过滤。
- 会话列表空状态文案已区分：
  - 初始列表为空时仍显示“暂无会话”。
  - 搜索无匹配结果时显示“没有匹配的会话”。
- 诊断页端口文案已中文化并补清上下文：
  - 摘要格式改为“Web Token 状态 · Token 来源 · 监听范围 · 端口 N”。
  - 运行列表行名从 `Port` 改为“端口”。
- 诊断刷新按钮已补充 `type="button"`。
- README 已补充会话列表本地搜索、诊断页白名单状态渲染、会话加载上限、无匹配文案和诊断摘要格式。
- `docs/browser-smoke.md` 已补充会话搜索无匹配/空列表文案和诊断页端口文案边界。
- `docs/runtime-runbook.md` 已新增“会话列表和诊断页边界”章节，记录 `/api/dialogs?limit=120`、前端本地搜索、会话项显示字段、诊断摘要、脱敏字段和错误 ID 行为。
- `tests/test_core.py` 和 `tests/frontend_smoke.js` 已同步会话/诊断文案断言，并新增纯 Node smoke 覆盖搜索无匹配文案。
- 会话加载、诊断采集、脱敏逻辑、鉴权、Socket.IO 和 API 行为未修改。

## 本阶段提交

- commit：提交信息 `phase33: align dialogs diagnostics copy`；精确 hash 以 `git log -1 --oneline` 为准。
- 主要文件：
  - `templates/chats.html`
  - `templates/diagnostics.html`
  - `static/js/app.js`
  - `tests/test_core.py`
  - `tests/frontend_smoke.js`
  - `README.md`
  - `docs/browser-smoke.md`
  - `docs/runtime-runbook.md`
  - `Telegram_Web开发.md`
  - `docs/progress/2026-07-05-phase-33-dialogs-diagnostics-copy.md`
  - `docs/handoff/2026-07-05-phase-33-to-phase-34.md`
  - `docs/handoff/LATEST.md`

## 已验证

- `sh -n scripts/diagnose-runtime.sh`：通过
- `sh scripts/diagnose-runtime.sh`：通过，`failures=0 warnings=0`
- `TELEGRAM_WEB_HOST=0.0.0.0 sh scripts/diagnose-runtime.sh`：通过，`failures=0 warnings=1`，warning 为未设置环境变量 Web Token
- `TELEGRAM_WEB_DIAGNOSTICS_URL=http://127.0.0.1:9/api/diagnostics sh scripts/diagnose-runtime.sh`：通过，HTTP 探测失败按 warning 处理
- `sh scripts/check-browser-smoke-env.sh`：通过；输出自动化浏览器 smoke 当前不可用，使用手动清单
- `PYTHONPYCACHEPREFIX="${TMPDIR:-$HOME/.cache}/telegram-web-pycache" python -m py_compile app.py`：通过
- `PYTHONPYCACHEPREFIX="${TMPDIR:-$HOME/.cache}/telegram-web-pycache" python -Wd -m unittest discover -v`：通过，56 个测试
- `node --check static/js/app.js`：通过
- `node --check tests/frontend_smoke.js`：通过
- `node tests/frontend_smoke.js`：通过，输出 `frontend smoke passed`
- `git diff --check`：通过
- `git diff --cached --check`：通过

## 未完成/风险

- 未执行真实浏览器或屏幕阅读器人工 smoke。
- 自动浏览器 smoke 仍未引入；如后续引入，需要单独新增 npm 元数据、锁文件、浏览器安装/缓存说明和可选验证边界。
- 会话搜索仍只过滤当前已加载的最近 120 个会话，不进行服务端搜索或 Telegram 全量查询。
- 诊断页仍依赖 `/api/diagnostics` 返回的白名单状态；运行数据、Telegram 凭据、session 文件和下载目录仍应保持不提交。

## 下阶段目标

Phase 34 建议主题：**全局导航和错误反馈文案边界复核**。

优先完成：

- 只读复查 `templates/base.html`、`static/js/app.js` 中顶部连接状态、刷新按钮、底部导航、toast、`api()` 错误 ID 展示/复制、401 跳转和 Web Token 鉴权提示相关文案，及 `README.md`、`docs/browser-smoke.md`、`docs/runtime-runbook.md` 和相关测试。
- 确认全局状态、导航、toast、确认弹窗、错误 ID、剪贴板复制失败容忍、`/auth` 跳转和 Web Token 提示的页面文案、文档和测试描述一致。
- 如发现页面提示、按钮标签、README、runbook 或手动 smoke 清单与实际行为不一致，优先做小范围文案/文档/断言修正。
- 不改变鉴权逻辑、Socket.IO 行为、API 错误包装、剪贴板权限处理或导航结构，除非发现现有文案对应的命令/行为明显失效。
- 不引入新依赖，不读取或提交 `Download/`、`Pictures/`、`data/`、`.session` 和任何真实 Token。

## 建议子代理拆分

- Explorer：只读梳理全局模板、前端 API/状态/toast 文案、README、runbook、browser smoke 和相关测试之间的一致性。
- Worker：如确有必要，限定修改文案和文档，不改鉴权、Socket.IO 或错误包装行为逻辑。
- Verifier：运行诊断脚本、py_compile、后端单测、Node smoke、浏览器环境检查和 `git diff --check`。

## 续会启动步骤

1. 阅读 `Telegram_Web开发.md`。
2. 阅读本文。
3. 运行 `git status --short` 和 `git log --oneline -5`。
4. 确认 Git 身份仍为 `liyw0205 <2650115317@qq.com>`。
5. 按“下阶段目标”启动 Phase 34。
6. 阶段结束前更新新的 `docs/progress/`、`docs/handoff/` 和 `docs/handoff/LATEST.md`，然后提交并推送 Git。
