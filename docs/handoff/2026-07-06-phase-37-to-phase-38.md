# Phase 37 -> Phase 38 交接

## 当前完成进度

- 登录页配置输入提示已与后端边界重新对齐：
  - `api_id`、`api_hash`、手机号、SOCKS4/5 代理、下载线程和缓存上限补齐或收紧了轻量 HTML 提示/隐藏辅助说明。
  - 代理提示明确端口范围、默认端口和禁止 path/query/fragment。
  - 敏感字段仍不回显真实 `api_hash`、StringSession、Web Token、含凭据代理或 `.session` 内容。
- 前端 smoke 增加登录页/API 覆盖：
  - 保存配置 payload 会携带 Web Token。
  - 发送验证码 payload 不携带 Web Token。
  - 后端中文配置错误通过 toast 展示。
- README、runbook、browser smoke 和主开发文档已同步 Phase 37 基线。
- 未改变登录流程、后端配置保存规则、session 导入导出、鉴权逻辑、Telethon 行为或依赖。

## 本阶段提交

- commit：提交信息 `phase37: align login input guidance`；精确 hash 以 `git log -1 --oneline` 为准。
- 主要文件：
  - `templates/login.html`
  - `tests/test_core.py`
  - `tests/frontend_smoke.js`
  - `README.md`
  - `docs/runtime-runbook.md`
  - `docs/browser-smoke.md`
  - `Telegram_Web开发.md`
  - `docs/progress/2026-07-06-phase-37-login-input-errors.md`
  - `docs/handoff/2026-07-06-phase-37-to-phase-38.md`
  - `docs/handoff/LATEST.md`

## 已验证

- `sh -n scripts/diagnose-runtime.sh`：通过
- `sh scripts/diagnose-runtime.sh`：通过，`failures=0 warnings=0`
- `TELEGRAM_WEB_HOST=0.0.0.0 sh scripts/diagnose-runtime.sh`：通过，`failures=0 warnings=1`，warning 为未设置环境变量 Web Token
- `TELEGRAM_WEB_DIAGNOSTICS_URL=http://127.0.0.1:9/api/diagnostics sh scripts/diagnose-runtime.sh`：通过，HTTP 探测失败按 warning 处理
- `sh scripts/check-browser-smoke-env.sh`：通过；输出自动化浏览器 smoke 当前不可用，使用手动清单
- `PYTHONPYCACHEPREFIX="${TMPDIR:-$HOME/.cache}/telegram-web-pycache" python -m py_compile app.py`：通过
- `PYTHONPYCACHEPREFIX="${TMPDIR:-$HOME/.cache}/telegram-web-pycache" python -Wd -m unittest discover -v`：通过，57 个测试
- `node --check static/js/app.js`：通过
- `node --check tests/frontend_smoke.js`：通过
- `node tests/frontend_smoke.js`：通过，输出 `frontend smoke passed`
- `rg -n "api_hash|StringSession|\\.session|Bearer\\s+[A-Za-z0-9._-]{16,}|sk-[A-Za-z0-9]|password\\s*[:=]" .`：通过；仅命中文档、字段名和测试假数据，未发现真实凭据
- `git diff --check`：通过
- `git diff --cached --check`：通过

## 未完成/风险

- 未执行真实浏览器或屏幕阅读器人工 smoke。
- HTML 输入提示不能替代后端校验；如果浏览器原生提示在移动端体验不佳，后续阶段应做小范围模板/CSS 修正。
- 自动浏览器 smoke 仍未引入；当前环境检查仍显示缺少 Playwright 模块和常见浏览器命令。
- 运行数据、Telegram 凭据、session 文件和下载目录仍应保持不提交。

## 下阶段目标

Phase 38 建议主题：**真实浏览器手动 smoke 条件和登录页提示回归收口**。

优先完成：

- 按 `docs/browser-smoke.md` 对 `/login` 做桌面和窄屏手动 smoke，重点看提示属性、脱敏占位符、toast、键盘焦点和按钮分组。
- 重新运行 `scripts/check-browser-smoke-env.sh`，确认是否仍不具备自动化浏览器 smoke 条件。
- 如仍缺 Playwright/浏览器命令，不引入新依赖，只更新手动 smoke 结论和阶段文档。
- 如发现真实浏览器中提示属性、布局或 toast 可见性问题，只做小范围 CSS/模板修正。
- 不改变登录流程、配置保存规则、session 导入导出、鉴权逻辑、Telethon 行为或新增依赖。

## 建议子代理拆分

- Explorer：只读复查 `docs/browser-smoke.md`、登录页模板、CSS 和当前浏览器自动化条件。
- Worker：如有必要，限定修改登录页模板/CSS 或 browser smoke 文档，不改后端 API。
- Verifier：运行诊断脚本、py_compile、后端单测、Node smoke、浏览器环境检查和 `git diff --check`。

## 续会启动步骤

1. 阅读 `Telegram_Web开发.md`。
2. 阅读本文。
3. 运行 `git status --short` 和 `git log --oneline -5`。
4. 确认 Git 身份仍为 `liyw0205 <2650115317@qq.com>`。
5. 按“下阶段目标”启动 Phase 38。
6. 阶段结束前更新新的 `docs/progress/`、`docs/handoff/` 和 `docs/handoff/LATEST.md`，然后提交并推送 Git。
