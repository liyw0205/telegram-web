# Phase 20 -> Phase 21 交接

## 当前完成进度

- 重新运行 `sh scripts/check-browser-smoke-env.sh`：
  - Node/npm/npx 可用。
  - Playwright Node 模块缺失。
  - `chromium`、`chromium-browser`、`google-chrome`、`google-chrome-stable`、`firefox` 命令缺失。
  - 当前 shell 仍不具备自动化真实浏览器 smoke 条件。
- 未新增 npm、Playwright、Puppeteer、Selenium 或浏览器安装步骤。
- `docs/browser-smoke.md` 已加入 `/diagnostics`：
  - 基础页面路径包含 `/diagnostics`。
  - 新增诊断页手动 smoke 清单。
  - 覆盖摘要、四组列表、刷新、错误状态、Web Token 跳转和移动端布局。
  - 明确不得显示原始 host、URL、Token、本地路径、手机号、代理原文、代理凭据、StringSession 或 `.session` 内容。
- `Telegram_Web开发.md` 已同步 Phase 20 记录。

## 本阶段提交

- commit：提交信息 `phase20: document diagnostics browser smoke`；精确 hash 以 `git log -1 --oneline` 为准。
- 主要文件：
  - `docs/browser-smoke.md`
  - `Telegram_Web开发.md`
  - `docs/progress/2026-07-05-phase-20-diagnostics-browser-smoke.md`
  - `docs/handoff/2026-07-05-phase-20-to-phase-21.md`
  - `docs/handoff/LATEST.md`

## 已验证

- `sh scripts/check-browser-smoke-env.sh`：通过；输出自动化浏览器 smoke 当前不可用，使用手动清单
- `sh -n scripts/diagnose-runtime.sh`：通过
- `sh scripts/diagnose-runtime.sh`：通过，`failures=0 warnings=0`
- `TELEGRAM_WEB_HOST=0.0.0.0 sh scripts/diagnose-runtime.sh`：通过，`failures=0 warnings=1`，warning 为未设置环境变量 Web Token
- `TELEGRAM_WEB_DIAGNOSTICS_URL=http://127.0.0.1:9/api/diagnostics sh scripts/diagnose-runtime.sh`：通过，HTTP 探测失败按 warning 处理
- `PYTHONPYCACHEPREFIX="${TMPDIR:-$HOME/.cache}/telegram-web-pycache" python -m py_compile app.py`：通过
- `PYTHONPYCACHEPREFIX="${TMPDIR:-$HOME/.cache}/telegram-web-pycache" python -Wd -m unittest discover -v`：通过，50 个测试
- `node --check static/js/app.js`：通过
- `node --check tests/frontend_smoke.js`：通过
- `node tests/frontend_smoke.js`：通过，输出 `frontend smoke passed`
- `git diff --check`：通过

## 未完成/风险

- 未执行真实浏览器人工 smoke；当前仓库仍以纯 Node smoke、Flask 测试和手动浏览器清单为边界。
- 如后续引入 Playwright，需要新增 `package.json`、锁文件、浏览器安装/缓存说明和可选验证命令。
- 运行数据、Telegram 凭据、session 文件和下载目录仍应保持不提交。

## 下阶段目标

Phase 21 建议主题：**诊断页可访问性和状态语义补强**。

优先完成：

- 只读复查 `templates/diagnostics.html`、`static/js/app.js` 和 `static/css/app.css` 的诊断页可访问性。
- 评估是否给诊断摘要和错误状态增加低风险的 `aria-live`、按钮 `aria-label` 或状态语义，不改变脱敏边界。
- 若修改前端，补充纯 Node smoke 或后端页面测试；继续不引入构建链。
- 保持诊断页只渲染白名单布尔、枚举和数值状态，不展示 secret、路径或 raw host。
- 阶段结束继续跑诊断脚本、py_compile、后端单测、Node smoke 和 `git diff --check`。

## 建议子代理拆分

- Explorer：只读梳理诊断页模板、渲染 helper、CSS 和现有 smoke，列出低风险可访问性缺口。
- Worker：如确有必要，限定修改诊断页模板、前端 helper/CSS 和 smoke 测试。
- Verifier：运行诊断脚本、py_compile、后端单测、Node smoke、浏览器环境检查和 `git diff --check`。

## 续会启动步骤

1. 阅读 `Telegram_Web开发.md`。
2. 阅读本文。
3. 运行 `git status --short` 和 `git log --oneline -5`。
4. 确认 Git 身份仍为 `liyw0205 <2650115317@qq.com>`。
5. 按“下阶段目标”启动 Phase 21。
6. 阶段结束前更新新的 `docs/progress/`、`docs/handoff/` 和 `docs/handoff/LATEST.md`，然后提交 Git。
