# Phase 10 -> Phase 11 交接

## 当前完成进度

- 完成 Phase 9 收尾并提交 `phase9: extend downloads frontend smoke`。
- Phase 10 重整 `tests/frontend_smoke.js`：
  - 统一 mock API 成功/失败/状态码响应。
  - 集中注册 mock DOM 元素。
  - 集中 HTML 和文本断言 helper。
- 新增覆盖：
  - `api()` 收到 `error_id` 时，错误消息包含错误 ID，并尝试复制到剪贴板。
  - `api()` 收到 401 时，跳转 `/auth?next=...`。
  - `loadLoginPage()` 对已保存敏感配置只展示脱敏 placeholder，不回填敏感值。
- README 和开发方案同步更新了前端 smoke 覆盖范围。

## 本阶段提交

- commit：提交信息 `phase10: improve frontend smoke coverage`；精确 hash 以 `git log -1 --oneline` 为准。
- 主要文件：
  - `tests/frontend_smoke.js`
  - `README.md`
  - `Telegram_Web开发.md`
  - `docs/progress/2026-07-05-phase-10-frontend-smoke-maintainability.md`
  - `docs/handoff/2026-07-05-phase-10-to-phase-11.md`
  - `docs/handoff/LATEST.md`

## 已验证

- `PYTHONPYCACHEPREFIX="${TMPDIR:-$HOME/.cache}/telegram-web-pycache" python -m py_compile app.py`：通过
- `PYTHONPYCACHEPREFIX="${TMPDIR:-$HOME/.cache}/telegram-web-pycache" python -Wd -m unittest discover -v`：通过，48 个测试
- `node --check static/js/app.js`：通过
- `node --check tests/frontend_smoke.js`：通过
- `node tests/frontend_smoke.js`：通过，输出 `frontend smoke passed`
- `git diff --check`：通过

## 未完成/风险

- 仍未执行真实浏览器点击回归；当前是 mock 浏览器环境。
- 前端 smoke 不验证 CSS 布局、真实事件冒泡、真实 Socket.IO 连接、媒体加载和浏览器剪贴板权限差异。
- 原生 `confirm()` 仍未替换为自定义可访问确认组件。
- 下载页 smoke 仍和当前 HTML 字符串结构耦合。
- 没有 systemd、Termux service、容器化或日志轮转脚本。

## 下阶段目标

Phase 11 建议主题：**敏感操作确认体验和真实浏览器 smoke 评估**。

优先完成：

- 评估是否用轻量自定义确认组件替换原生 `confirm()`，覆盖退出登录、session 导入导出、任务取消/移除。
- 若实现自定义确认组件，保持无构建链，并补充纯 Node smoke 覆盖确认、取消、键盘关闭和并发点击边界。
- 评估当前 Termux 环境是否可运行 Playwright 或其他真实浏览器 smoke；如果成本过高，只记录结论，不引入依赖。
- 保持真实凭据、session 文件和运行数据不进入提交。

## 建议子代理拆分

- Explorer：只读梳理所有 `confirmSensitive()` 调用、按钮入口和可访问性需求。
- Worker：实现或调整确认组件及对应 smoke，限定修改 `templates/base.html`、`static/js/app.js`、`static/css/app.css`、`tests/frontend_smoke.js`。
- Verifier：运行 py_compile、单元测试、Node smoke、`git diff --check`，确认无运行数据进入提交。

## 续会启动步骤

1. 阅读 `Telegram_Web开发.md`。
2. 阅读本文。
3. 运行 `git status --short` 和 `git log --oneline -5`。
4. 确认 Git 身份仍为 `liyw0205 <2650115317@qq.com>`。
5. 按“下阶段目标”启动 Phase 11。
6. 阶段结束前更新新的 `docs/progress/`、`docs/handoff/` 和 `docs/handoff/LATEST.md`，然后提交 Git。
